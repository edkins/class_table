class Foo {
    a : u32,
    b : u32,
}

impl Foo:: {
    fn sum(self) -> str {
        self.a + self.b
    }
}

fn main(input: str) -> str {
    let x = Foo {
        a : 1,
        b : 2,
    }
    assert(x.sum() == 3);
    "ok"
}