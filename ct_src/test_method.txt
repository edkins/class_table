class Foo {
    a : u32,
    b : u32,
}

impl Foo:: {
    fn sum(self) -> u32 {
        self.a + self.b
    }
}

fn main(input: str) -> str {
    let x = Foo {
        a : 1u32,
        b : 2u32,
    };
    assert(x.sum() == 3u32);
    "ok"
}