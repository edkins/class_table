trait greeting {
    fn greet(self : _) -> str;
}

impl u32::greeting {
    fn greet(self : _) -> str {
        "hello"
    }
}

impl str::greeting {
    fn greet(self : _) -> str {
        "goodbye"
    }
}

fn main(input: str) -> str {
    let x = 42u32;
    let y = "string";
    assert(x.greet() + y.greet() == "hellogoodbye");
    "ok"
}