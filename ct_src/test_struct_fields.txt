class Foo {
    a : str,
    b : str,
}

fn main(input: str) -> str {
    let x = Foo {
        a : "a",
        b : "b",
    };
    let y = struct[str]{x};
    let mut result = "";
    for name : y.field_names() {
        result += name;
    }
    assert(result == "ab");
    "ok"
}