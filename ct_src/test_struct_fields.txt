use introspect.examine_struct;

class Foo {
    a : str,
    b : str,
}

fn main(input: str) -> str {
    let x = Foo {
        a : "a",
        b : "b",
    };
    let mut result = "";
    for name : x.field_names() {
        result += name;
    }
    assert(result == "ab");
    "ok"
}